<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.imagesMapper">

	<select id="selectAllByModelId" resultType="String">
		<![CDATA[
			SELECT i.imglink
			FROM images i 
				JOIN model m ON i.modelid = m.id
			ORDER BY m.rating DESC
			LIMIT 6
		]]>
	</select>
	
	<select id="selectByModelId" resultType="String" parameterType="int">
		<![CDATA[
			SELECT imglink
			FROM images
			WHERE modelid = #{modelid}
		]]>
	</select>

</mapper>